/**
 * Citation extractor.
 *
 * Parses the LLM response to extract evidence references ([1], [2], etc.)
 * and maps them back to document IDs and scores.
 *
 * Also responsible for building the message_citations DB records.
 *
 * TODO(week-5): implement
 */

export {};
